class Car 
{ 
    String vehicle_id; 
    float [] Cost; // stores base, exercise duty and sales tax for each car. 
    float total; 
    float Grand_Total; 

    calc_total(total, base, exercise_duty, sales_tax)
    { 
        total = base + exercise_duty + sales_tax; 
        return total; 
    }

    
}

public class Q1
{ 
    public static void main (String[] args)
    { 
        int total = 0; 

        System.out.println ("Enter name of the vehicle: "); 
        System

        total = calc_grand_total (int total, float Cost[0], float Cost[1], float Cost[2]); 
        Grand_Total = calc_grand_total(int total); 
    }
}